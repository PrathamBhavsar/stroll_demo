import 'package:flutter/material.dart';

class AppSpaces {
  static EdgeInsets appPadding = const EdgeInsets.all(16);

  static Widget gapH(double h) => SizedBox(height: h);
  static Widget gapW(double w) => SizedBox(width: w);
}
